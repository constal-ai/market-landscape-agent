{
  "name": "@constal/market-landscape-agent",
  "version": "0.3.0",
  "description": "A Constal agent that researches a market landscape from a natural-language request.",
  "license": "Apache-2.0",
  "author": "Coresource AI, Inc.",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit --pretty false -p tsconfig.json",
    "test": "vitest run",
    "check": "npm run typecheck && npm test",
    "ui:build": "node scripts/build-ui.mjs",
    "ui:preview": "node scripts/preview-ui.mjs",
    "ui:preview:live": "node scripts/preview-ui.mjs --live",
    "ui:deploy": "node scripts/deploy-ui.mjs"
  },
  "dependencies": {
    "@constal/sdk": "3.4.0"
  },
  "devDependencies": {
    "@constal/cli": "1.3.1",
    "typescript": "5.9.3",
    "vitest": "3.2.7"
  }
}
